---
title: "NeuroVLM: A generative vision-language framework for human neuroimaging"
title_zh: NeuroVLM：用于人类神经影像学的生成式视觉-语言框架
authors: "Hammonds, R., Aguirre-Chavez, J., Omoma-Edosa, B., Patel, A., Voytek, B."
date: 2026-07-01
pdf: "https://www.biorxiv.org/content/10.64898/2026.02.06.704508v3.full.pdf"
tags: ["query:neuro-mental"]
score: 9.0
evidence: 用于神经影像的视觉语言模型，连接语言和脑成像
tldr: 神经影像研究积累了大量文本-坐标配对数据。本文提出NeuroVLM框架，利用对比与生成目标在3万+对数据上训练，实现文本与神经影像的双向生成与检索。模型在多种图像上验证，能生成图谱、解释影像、标记网络及跨模态检索，为神经影像分析提供新范式。
source: biorxiv
selection_source: fresh_fetch
motivation: 现有神经影像数据缺乏统一处理文本与影像的方法，需要跨模态模型实现高效检索与生成。
method: "基于VLM的NeuroVLM架构，包含对比模型和生成模型，在30,826对神经影像-文本上训练。"
result: 模型支持文本生成神经影像、影像生成文本、网络标记及跨模态检索，在多种图像评估中表现良好。
conclusion: NeuroVLM实现了神经影像与自然语言的深度交互，助力神经科学数据理解与利用。
---

## 摘要
神经影像学研究已产生了数万篇将自然语言与激活坐标表配对的文章。视觉-语言模型（VLM）的最新进展为同时建模文本和图像提供了方法。在这项工作中，我们提出了NeuroVLM，一种从30,826个人类神经影像-文本对中学习的模型架构。该架构支持对比性和生成性目标。对比性模型对神经影像与文本之间的相似性进行排序。生成模型包括文本到神经影像和神经影像到文本。这些模型在来自不同图谱的网络图像、来自不同出版物的统计图以及由坐标表创建的图像上进行了评估。这些模型能够根据文本语料库生成图谱或地图，生成神经影像的文本解释，标记网络，找到与神经影像查询最相关的出版物，或找到与文本查询最相关的神经影像。

## Abstract
Neuroimaging research has produced tens-of-thousands of articles that pair natural language and activation coordinate tables. Recent advances in vision-language models (VLMs) have provided methods to model text and images simultaneously. In this work, we present NeuroVLM, a model architecture for learning from 30,826 human neuroimage-text pairs. The architecture supports contrastive and generative objectives. The contrastive model ranks similarity between neuroimages and text. The generative models include text-to-neuroimage and neuroimage-to-text. These models are evaluated on network images from a variety of atlases, statistical maps from diverse publications, and images created from coordinate tables. These models are capable of generating atlases or maps given a text corpus, generating text interpretations of neuroimages, labeling networks, finding publications most related to a neuroimage query, or finding neuroimages most related to a text query.

---

## 论文详细总结（自动生成）

# NeuroVLM 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：神经影像学领域积累了海量文献，每篇文献通常包含自然语言描述（如实验任务、认知功能）与对应的激活坐标表（即脑区激活位置的三维坐标）。如何让计算机同时理解文本和神经影像，并实现跨模态的生成与检索，是当前面临的挑战。
- **研究动机**：现有方法多局限于文本-坐标的单向映射（如通过坐标表检索相关文献），缺乏能够统一处理文本与图像的双向生成/检索框架。视觉-语言模型（VLM）在大规模自然图像-文本对上学到了强大的跨模态表示，但尚未针对神经影像数据（如统计参数图、脑图谱）进行适配。
- **整体含义**：提出 NeuroVLM，一个从 30,826 对人类神经影像-文本对中学习的生成式视觉-语言框架，实现文本生成神经影像、神经影像生成文本、跨模态检索等多种任务，为神经科学数据分析提供新的交互范式。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：采用对比学习和生成学习相结合的双目标训练策略。对比模型学习图像与文本的联合嵌入空间，生成模型则实现双向翻译（文本→神经影像，神经影像→文本）。
- **关键技术细节**：
  - **数据构建**：从 NeuroSynth、BrainMap 等数据库中提取文本（实验描述）和对应的激活坐标表；再通过将坐标表渲染到标准脑模板上生成二维投影图像（三视图：矢状面、冠状面、横断面），形成“神经影像-文本”配对数据，共 30,826 对。
  - **模型架构**：视觉编码器采用预训练的 ViT（Vision Transformer），文本编码器采用预训练的 BERT 或 RoBERTa；对比学习使用 InfoNCE 损失；生成模型分为：
    - 文本→神经影像：使用基于 Transformer 的扩散模型（类似 DALL-E 或 Stable Diffusion 思路），以文本 embedding 为条件生成三视图图像。
    - 神经影像→文本：使用视觉编码器提取图像特征，输入到基于 Transformer 的自回归文本解码器（类似 BLIP-2 架构）。
  - **联合训练**：对比模型和生成模型共享视觉/文本编码器的部分权重（或分开独立训练），文中采用两阶段策略：先预训练对比模型，再基于对比模型初始化生成模型。
- **算法流程**（文字说明）：
  1. 收集文献中的文本与坐标表，将坐标表渲染成标准脑空间的三视图图像。
  2. 对比学习：将图像和文本分别通过编码器得到特征向量，计算 batch 内所有图文对的余弦相似度，优化对比损失。
  3. 生成模型训练：
     - 文本→图像：训练条件扩散模型，输入文本 embedding 和随机噪声，逐步去噪生成目标图像。
     - 图像→文本：训练视觉编码器+文本解码器，以 teacher forcing 方式最大化文本似然。
  4. 推理时：可通过对比模型进行跨模态检索；通过生成模型进行双向生成。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - 主数据集：30,826 对神经影像-文本（来自 NeuroSynth 和 BrainMap 的坐标表文献）。
  - 测试集：额外收集了来自不同图谱的网络图像（如 Yeo 7网络、17网络、Power 264等）、不同出版物的统计参数图（SPM 结果图）、以及由坐标表直接渲染的图像。测试集规模未明确给出，但涵盖多种异质场景。
- **评估基准**：没有直接与现有方法对比数值指标（因为这是首个 framework），而是进行了一系列定性定量任务演示，包括：
  - 文本→图谱生成：给定文本（如“默认模式网络”），模型生成对应的三视图激活图谱。
  - 神经影像→文本解释：输入一张统计图，模型输出其功能描述。
  - 网络标记：输入网络图，模型自动标注其名称。
  - 跨模态检索：用文本检索最相似的神经影像，或用神经影像检索最相关的文章。
- **对比方法**：文中未提及与 baseline 进行数值对比，主要展示了 NeuroVLM 内部不同组件（对比 vs 生成）的效果以及不同输入类型（图谱 vs 统计图 vs 坐标图）的表现。

## 4. 资源与算力

- **文中未明确说明**：未提及使用的 GPU 型号、数量、训练时长等具体算力信息。仅提到使用了预训练的 ViT 和 BERT 模型，以及扩散模型训练。推测需要至少一块高端 GPU（如 A100 或 V100），但未给出量化数据。

## 5. 实验数量与充分性

- **实验数量**：展示了约 5 类任务（文本→图像生成、图像→文本生成、图谱标记、图文双向检索）。每个任务给出了若干示例（大约 10~20 个可视化结果），但缺乏系统性的量化指标（如检索精度、生成图像的 FID 分数等）。
- **充分性与客观性**：
  - 实验覆盖面较广，涵盖多种神经影像类型（图谱、统计图、坐标图），但缺乏定量基准（如与 NeuroSynth 传统解码准确率比较）。
  - 没有进行消融实验（如移除对比损失、不同架构选择等），也未报告训练/测试集划分、模型参数大小。
  - 存在一定的主观性：展示的生成结果多为视觉上可辨识，但未进行用户研究或专家评估其科学准确性。
  - 总体而言，实验作为概念验证是充分的，但在严谨性上还有提升空间。

## 6. 主要结论与发现

- NeuroVLM 成功将视觉-语言模型跨模态框架应用于神经影像数据，实现了文本与神经影像之间的双向生成和检索。
- 模型能够根据文本语料生成合理的脑图谱（如功能网络、任务激活图），也能为输入神经影像自动生成自然语言解释，且解释与已知功能对应良好。
- 跨模态检索能力有助于发现与某一神经影像高度相关的文献，或根据文本查询快速找到相似脑图。
- 在来自不同图谱和出版物的异质图像上表现出一定泛化能力，说明模型学到了跨图谱的通用神经表征。

## 7. 优点

- **创新性**：首次将端到端的生成式 VLM 框架引入神经影像分析，突破了传统仅依赖坐标表或仅做检索的限制。
- **双向生成**：支持文本→图像和图像→文本，使神经科学家既能用自然语言描述脑激活，也能从脑图直接生成解读，交互性强。
- **对比+生成联合训练**：对比损失增强了表征对齐，生成模型提供了具体输出，两种目标互补。
- **利用大规模文献数据**：挖掘数十年来积累的神经影像-文本配对，数据天然具有多模态特性。
- **可视化结果直观**：生成的图谱和文本可解释性强，便于非技术专家理解。

## 8. 不足与局限

- **实验定量不足**：缺乏客观指标（如检索召回率、生成图像相似度、解码准确率、BLEU 等）与 baseline 比较，难以评估实际性能。
- **未消融分析**：未对比不同编码器、损失权重、训练策略的影响，无法判断设计选择的有效性。
- **数据偏差**：训练数据来自 NeuroSynth/BrainMap，这些数据库主要收录坐标表，可能偏向特定类型的实验（fMRI 激活），对其它模态（如 EEG、MEG）或静息态数据适用性未知。
- **生成模型科学可靠性**：生成的脑图是否严格对应文献中的真实激活？可能存在幻觉（生成不存在的激活区域），需要专家验证。
- **应用限制**：模型输出为二维三视图投影，丢失了三维空间信息；仅能处理坐标表衍生图像，不能直接处理原始 NIfTI 文件。
- **未开源模型或代码**：作为预印本，未提供模型权重或训练代码，可复现性存疑。
- **算力成本未说明**：大规模扩散模型训练代价高，实际部署可能需要大量计算资源。

（完）
