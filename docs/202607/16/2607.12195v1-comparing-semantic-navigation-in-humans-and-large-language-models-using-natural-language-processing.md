---
title: Comparing Semantic Navigation in Humans and Large Language Models using Natural Language Processing
title_zh: 使用自然语言处理比较人类与大语言模型中的语义导航
authors: "Gabriel Paris-Colombo, Rodrigo M. Cabral-Carvalho, Felipe D. Toro-Hernández"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.12195v1"
tags: ["query:neuro-mental"]
score: 8.0
evidence: 使用NLP比较人类和LLM的语义导航
tldr: 研究通过言语流畅性任务，比较人类与GPT-4o等LLM的语义记忆检索动态。利用轨迹NLP指标分析发现，人类在语义空间中表现出更高的熵、更大的跳跃步长和更广的分散，而LLM的搜索模式更为保守。即使调整温度参数，LLM也无法完全匹配人类在所有维度上的特征，揭示两者语义导航的本质差异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12195v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 857, \"height\": 1626, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12195v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 905, \"height\": 932, \"label\": \"Table\"}]"
motivation: 探究人类与LLM在语义记忆检索中的导航策略差异，为理解模型与人类认知差距提供依据。
method: 收集82名人类被试和三种LLM在八种温度设置下的言语流畅性数据，应用轨迹NLP指标量化语义搜索的熵、步长和分散度。
result: 人类在所有指标上均高于LLM；温度调节仅能使单一指标对齐，无法复现完整的人类特征。
conclusion: 人类语义搜索具备独特的局部-全局平衡，当前LLM架构尚未能再现这种认知模式。
---

## 摘要
语义记忆检索可以被概念化为在概念空间中的导航。我们利用言语流畅性数据，比较了人类与三个大语言模型（GPT-4o、Gemini-2.5-Pro、Claude-Sonnet-4.5）之间的语义搜索动态。通过将基于轨迹的NLP指标应用于82名人类参与者生成的条目以及八个温度设置下的大语言模型输出，我们量化了三个互补维度：熵（步长可预测性）、到下一项的距离（连续语义步长）以及到中心的距离（全局离散度）。人类在所有大语言模型上表现出更高的熵、更大的语义步长和更广的离散度，表明搜索更具变异性和探索性。温度调参仅产生了部分对齐，因为在特定设置下个别指标在人类与大语言模型之间匹配，但没有任何配置能完全再现人类的完整特征（在所有维度上）。这些发现表明，人类语义搜索实现了局部利用与全局探索之间的独特平衡，而当前的模型架构无法再现这一平衡。

## Abstract
Semantic memory retrieval can be conceptualized as navigation through conceptual space. We compared semantic search dynamics between humans and three large language models (GPT-4o, Gemini-2.5-Pro, Claude-Sonnet-4.5) using verbal fluency data. By applying trajectory-based NLP metrics to the items generated by 82 human participants and LLM output across eight temperature settings, we quantified three complementary dimensions: entropy (step size predictability), distance to next (successive semantic steps), and distance to centroid (global dispersion). Humans exhibited higher entropy, larger semantic steps and broader dispersion than all LLMs, indicating more variable and exploratory search. Temperature tuning produced only partial alignments, as individual metrics matched between humans and LLMs at specific settings, but no configuration reproduced the complete human profile (in all dimensions). These findings suggest that human semantic search implements a distinctive balance between local exploitation and global exploration that current model architectures fail to reproduce.

---

## 论文详细总结（自动生成）

# 论文总结：Comparing Semantic Navigation in Humans and Large Language Models using Natural Language Processing

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：人类语义记忆检索被视为概念空间中的导航过程，经典模型（如双重成分模型、最优觅食理论）强调局部聚类与全局切换的平衡。近年来，大语言模型（LLM）在语言生成上表现类人，但它们在语义搜索的动态特性上是否真正接近人类尚不清楚。现有研究多关注LLM语义网络的静态结构差异（如弱聚类、高模块化），缺乏对逐时刻搜索轨迹的细粒度比较。
- **核心问题**：人类与LLM在言语流畅性任务中的语义导航动态（步长可预测性、连续步长大小、全局离散度）是否存在系统性差异？温度调节能否使LLM匹配人类的多维特征？
- **整体含义**：揭示当前LLM虽能产生流畅输出，但其搜索模式（更保守、更局部、更可预测）与人类（更可变、更大步、更分散）本质不同，挑战了将LLM直接作为“硅参与者”用于认知研究的假设。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：借鉴Toro-Hernández等人（2026）的物理启发式NLP指标，将言语流畅性序列视为语义空间中的轨迹。使用**累积嵌入表征**（每一步将当前及之前所有条目拼接后编码）来模拟工作记忆累积约束，然后计算三种轨迹指标。
- **关键技术细节**：
  - **嵌入模型**：使用OpenAI的`text-embedding-3-large`将每个条目映射到高维向量，并用其他嵌入系统（商业与开源）验证鲁棒性。
  - **三种指标**：
    1. **距离到下一项（Distance to Next）**：计算连续累积状态向量之间的余弦距离，均值作为指标，反映连续语义步长大小。
    2. **熵（Entropy）**：对每个轨迹内的步长序列按中位数二值化（高/低），然后计算归一化香农熵，反映步长模式的可预测性。
    3. **距离到质心（Distance to Centroid）**：使用非累积的独立条目嵌入，计算每个条目到类别质心（所有独特条目嵌入的平均）的余弦距离，均值作为指标，反映语义离散度。
- **算法流程**（文字描述）：
  1. 对每一试次（人类或LLM），获得条目序列。
  2. 对条目进行累积拼接（如第t步为前t个条目的拼接文本），然后用嵌入模型编码。
  3. 计算相邻累积向量余弦距离→步长序列。
  4. 对步长序列二值化→计算熵。
  5. 对每个条目独立嵌入，计算所有独特条目嵌入的质心，再计算每个条目到质心的距离→取均值。
  6. 使用广义线性混合模型（GLMM）比较人类与各LLM（含温度条件）的组间差异。

## 3. 实验设计

- **数据集与场景**：
  - **人类数据**：来自SNAFU数据库（Zemla et al., 2020），82名人类被试完成“动物”类别言语流畅性任务（3分钟，避免重复与同后缀）。
  - **LLM数据**：三个模型（GPT-4o、Gemini-2.5-Pro、Claude-Sonnet-4.5）在8个温度设置（0.0, 0.15, 0.3, 0.45, 0.6, 0.75, 0.9, 1.0）下生成动物列表。为避免时间限制不可比，采用匹配长度策略：从人类条目数经验分布中采样目标长度。
  - **角色扮演诱导**：使用30种不同职业作为提示，先让模型描述该职业特征，再要求扮演该职业的人进行言语流畅性任务，以增加输出多样性。
- **Benchmark**：以人类群体的各指标分布作为基准。
- **对比方法**：无其他对比方法，主要比较人类 vs. 各LLM在不同温度下的差异，以及LLM之间的两两比较。

## 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量或训练时长。仅提及使用了OpenAI的嵌入API（`text-embedding-3-large`）和三个闭源LLM的API生成数据，以及R语言`glmmTMB`包进行统计分析。无自训练或微调环节，因此算力消耗主要集中在API调用和统计分析上。

## 5. 实验数量与充分性

- **实验数量**：
  - **人类组**：82名被试，每个被试一个试次。
  - **LLM组**：3个模型 × 8个温度 × 30个职业 = 720个试次（但实际每个试次输出长度与人类匹配，因此每个条件约82个输出？需注意：文中说对每个LLM试次，从人类条目数分布中采样目标长度，但未明确每个温度下的试次数。推测每个温度下每个模型使用30个职业各一次，共30个试次/温度/模型）。
  - 共进行1次类别间比较（人类 vs. LLM汇总）和8次温度对比（每个温度下人类 vs. 各LLM）。统计使用Tukey事后检验。
- **充分性**：
  - **优点**：覆盖了多个主要闭源LLM家族，温度范围从0到1.0，包括极端与中间值；使用了累积嵌入与多种指标进行多维比较；进行了鲁棒性验证（交替嵌入系统）。
  - **不足**：
    - 仅使用单一语义类别（动物），未扩展到其他类别（如水果、交通工具）。
    - LLM输出长度通过匹配策略控制，但未模拟人类的时间压力（人类3分钟，LLM无时间限制），可能引入并发效应干扰。
    - 未进行消融实验（如是否使用角色扮演、不同提示策略的影响）。
    - 统计方法依赖GLMM，但未报告效应量或模型诊断细节（仅提及使用DHARMa残差诊断）。

## 6. 论文的主要结论与发现

- **人类 vs. LLM整体差异**：人类在三个指标上均显著高于所有LLM（更高的熵、更大的距离到下一项、更大的距离到质心），表明人类搜索更不可预测、步长更大、离散度更广。LLM则更保守、局部化、中心化。
- **温度调节效果**：温度影响模型特定但非单调。个别指标在特定温度下与人类无显著差异，但**没有一种温度设置能同时使三个指标对齐人类**。全局离散度（距离到质心）最难恢复，GPT-4o在所有温度下均显著低于人类。
- **模型间差异**：Claude-sonnet-4.5最局部（最小步长、最低熵），Gemini-2.5-Pro居中，GPT-4o最集中（最大聚类）。这些差异在跨温度汇总后依然存在，反映架构与训练差异。
- **核心启示**：人类语义搜索实现了局部利用与全局探索的独特平衡，当前的LLM架构（即使调整温度）无法整体复现，表明它们缺乏人类那种受控随机性、灵活切换和广泛语义覆盖的能力。

## 7. 优点

- **方法创新**：采用累积嵌入表征，突破传统独立编码，更好地模拟工作记忆的动态约束，更具认知合理性。
- **多维测量**：同时考察步长、可预测性、离散度三个互补维度，避免了单一指标的片面性。
- **温度系统扫描**：覆盖8个温度点，揭示非线性关系，避免了简单“最佳温度”的误解。
- **鲁棒性验证**：使用多种嵌入系统（商业+开源）确认结果一致性，降低了对单一模型的依赖。
- **理论连接**：结果与双重成分模型、Lévy觅食模型、最优觅食理论紧密结合，为认知理论提供实证。

## 8. 不足与局限

- **实验覆盖有限**：仅使用一个语义类别（动物），未验证跨类别泛化性。人类数据来自公开数据库，可能引入实验环境差异（如3分钟 vs. 无时间限制）。
- **嵌入偏差**：所有嵌入模型均为训练产物，其几何结构可能与人类真实语义组织存在偏差。作者虽验证了替代系统，但本质上仍依赖计算表征。
- **闭源模型黑箱**：GPT-4o、Gemini、Claude均为闭权模型，无法分析内部层表征与行为的关系，限制机制解释。
- **温度调参的局限性**：仅调整温度一个参数，未探索其他生成参数（如top-p、top-k、重复惩罚等）的影响。
- **统计报告不完整**：文中未提供效应量、置信区间宽度、模型拟合诊断等细节，仅以p值显著性判断，可能忽略实际差异大小。
- **匹配长度策略的潜在问题**：人类输出受时间压力限制，LLM无时间限制但强制匹配长度，可能改变LLM的内在搜索动力学（如早期易生成常见项目，后期被迫生成不常见项目）。
- **角色扮演诱导的混淆**：使用30种职业可能引入主题偏差，不同职业可能激活不同语义子空间，但未分析其影响。

（完）
