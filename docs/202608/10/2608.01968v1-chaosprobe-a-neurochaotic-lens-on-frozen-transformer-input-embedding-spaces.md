---
title: "ChaosProbe: A Neurochaotic Lens on Frozen Transformer Input-Embedding Spaces"
title_zh: ChaosProbe：冻结Transformer输入嵌入空间的神经混沌透镜
authors: "Kunal Kumar Pant, Nithin Nagaraj"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.01968v1"
tags: ["query:neuro-mental"]
score: 8.0
evidence: 针对大语言模型的Transformer嵌入分析
tldr: Transformer模型通常从性能角度被理解，而冻结输入嵌入空间本身的结构缺乏直接探测手段。ChaosProbe方法利用确定性混沌轨迹变换，将每个提示级嵌入矩阵转换为由Firing Rate、熵等通道响应构成的固定长度指纹。在80条中性提示与GPT-2、DistilGPT2、BERT-base、RoBERTa-base四模型上，皮尔逊、斯皮尔曼、余弦相似度均完全恢复同族最近邻分配及预期互族配对，欧氏距离部分恢复，bootstrap配对检验证实配对稳定性。该工作为解析嵌入空间结构提供了任务无关的新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01968v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1772, \"height\": 809, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01968v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1788, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01968v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1789, \"height\": 760, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01968v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 890, \"height\": 787, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01968v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 892, \"height\": 520, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01968v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 655, \"height\": 202, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01968v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 836, \"height\": 465, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01968v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 841, \"height\": 168, \"label\": \"Table\"}]"
motivation: 现有模型分析多聚焦性能，缺乏从响应角度刻画冻结输入嵌入空间的方法。
method: 将嵌入矩阵经混沌轨迹变换后，提取Firing Rate和熵通道响应，生成固定长度签名。
result: 实验在80提示与四模型上，皮尔逊、斯皮尔曼、余弦相似度均恢复全部同族匹配，欧氏距离仅部分恢复。
conclusion: 该方法能有效暴露嵌入空间家族结构，可作为模型选择与解释的辅助工具。
---

## 摘要
Transformer模型通常通过其行为来理解：基准性能、生成质量或下游任务上的表现。然而，冻结的Transformer输入嵌入空间也可以通过它们在受控确定性探针下的响应来检验，这种检验发生在上下文计算或任务特定适应之前。基于这种响应视角，我们引入了ChaosProbe，一种受神经混沌启发的确定性方法，用于构建冻结Transformer输入嵌入空间的基于响应的指纹。对于每个提示级别嵌入矩阵，ChaosProbe应用基于混沌轨迹的变换，并通过互补的表示级度量总结其放电率和熵通道响应，为每个模型生成固定长度的签名。在一项有限的概念验证研究中，使用80个中性提示和四个预训练模型——GPT-2、DistilGPT2、BERT-base-uncased和RoBERTa-base——皮尔逊相关系数、斯皮尔曼相关系数和余弦相似度均恢复了所有四个同族最近邻分配以及两个预期的互为家族配对。欧几里得距离恢复了四个分配中的三个和两个互族配对中的一个。配对自助重采样支持皮尔逊和斯皮尔曼配对在观察到的提示集上的稳定性，签名有效性检查表明，常数或塌缩响应不会主导所报告的指纹。这些结果提供了一个依赖群组的概念验证，表明确定性神经混沌响应签名可以揭示冻结Transformer输入嵌入空间之间的广泛结构。

## Abstract
Transformer models are most often understood through what they do: their benchmark performance, generation quality, or behavior on downstream tasks. Yet frozen transformer input-embedding spaces may also be examined through their responses to a controlled deterministic probe before contextual computation or task-specific adaptation. Guided by this response-based view, we introduce \emph{ChaosProbe}, a deterministic neurochaos-inspired method for constructing response-based fingerprints of frozen transformer input-embedding spaces. For each prompt-level embedding matrix, ChaosProbe applies a chaotic trajectory-based transformation and summarizes its Firing Rate and Entropy channel responses with complementary representation-level measures, producing a fixed-length signature for each model. In a bounded proof-of-concept study of $80$ neutral prompts and four pretrained models---GPT-2, DistilGPT2, BERT-base-uncased, and RoBERTa-base---Pearson correlation, Spearman correlation, and cosine similarity each recover all four same-family nearest-neighbor assignments and both expected mutual family pairs. Euclidean distance recovers three of the four assignments and one of the two mutual family pairs. Paired bootstrap resampling supports the stability of the Pearson and Spearman pairings over the observed prompt set, and signature-validity checks show that constant or collapsed responses do not dominate the reported fingerprints. These results provide a cohort-dependent proof of concept that deterministic neurochaotic response signatures can expose broad structure among frozen transformer input-embedding spaces.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：现有Transformer模型分析大多聚焦于行为表现（基准性能、生成质量、下游任务），而冻结输入嵌入空间本身的结构缺乏直接、任务无关的刻画手段。论文提出一种“响应式”视角：将冻结输入嵌入空间视作一个可以被确定性非线性探针“询问”的几何对象，通过其响应模式而非坐标内容来表征模型。
- **整体含义**：ChaosProbe提出了一种受神经混沌学习（NL）启发的确定性框架，可将不同词汇表、分词器、嵌入维度的模型映射到统一的固定长度响应签名空间，从而在不依赖下游任务或分类器的情况下揭示模型之间的结构相似性（如家族关系）。这为嵌入空间的表征分析提供了新的方法论方向。

## 2. 论文提出的方法论

- **核心思想**：利用一维广义Lüroth级数（GLS）混沌映射（具体为斜帐篷映射）生成确定性轨迹，将每个嵌入坐标作为刺激与轨迹匹配，基于匹配前的轨迹段提取描述子（Firing Rate、Entropy），再聚合为表示级度量，最终形成模型级响应指纹。
- **关键技术细节与流程**：
  1. **输入构造**：对每个模型 m 和提示 p，经分词器与冻结嵌入表得到提示级嵌入矩阵 \(X_{m,p} \in \mathbb{R}^{T_{m,p} \times d_m}\)。
  2. **归一化**：对每个矩阵做全局最小-最大归一化至 [0,1]。
  3. **混沌探针**：固定参数网格 \(Q=\{0.06,0.16,\dots,0.96\}\)（10个）、\(B=\{0.06,\dots,0.46\}\)（5个）、\(\epsilon=\{0.06,0.16\}\)（2个），共 100 个配置。从 \(A_0=Q_c\) 开始迭代斜帐篷映射 \(A_{n+1}=G_{B_c}(A_n)\)，轨迹长度 L=1000。对每个标量坐标 x，找到第一个满足 \(|A_n-x|<\epsilon_c\) 的索引 \(\tau_L\)，取轨迹段 \((A_0,\dots,A_{\tau_L-1})\)。
  4. **描述子通道**：对每个归一化坐标计算 Firing Rate（阈值 B_c 以上比例）和 Entropy（阈值化符号序列的二元香农熵），形成两个与输入同形的描述子矩阵 \(D^{FR}\) 与 \(D^{ENT}\)。
  5. **表示级度量**：每个描述子矩阵计算四个度量：
     - 平均余弦相似度（与原始归一化矩阵逐行比较）；
     - 线性CKA（居中后比较关系结构）；
     - 各向异性（描述子行向量间平均余弦）；
     - 归一化有效秩（基于奇异值谱的熵归一化）。
  6. **签名生成**：对 100 个配置 × 2 个通道 × 4 个度量，在 80 个提示上取平均，按固定顺序向量化，得到 800 维模型响应签名 \(s_m\)；随后在参与比较的模型集合上做标准化，得到 \(z_m\)。
  7. **签名比较**：使用皮尔逊相关、斯皮尔曼相关、余弦相似度、欧氏距离计算模型间相似性，并评估最近邻家族恢复情况。

## 3. 实验设计

- **模型**：GPT-2、DistilGPT2（GPT家族）；BERT-base-uncased、RoBERTa-base（掩码编码器家族）。共 4 个冻结预训练检查点。
- **提示集**：80 条中性英文提示，分四类（事实性、推理、数学、对话），每类 20 条。不评估生成质量或任务精度。
- **基线/对比方法**：无传统机器学习基线；比较方法为四种签名相似性度量（皮尔逊、斯皮尔曼、余弦、欧氏距离），相互对比。
- **评估指标**：
  - 最近邻家族准确率（同族分配数量/总模型数）；
  - 互最近邻配对恢复（GPT-2↔DistilGPT2、BERT↔RoBERTa）；
  - 200 次配对 bootstrap 重采样（仅对皮尔逊和斯皮尔曼）的稳定性；
  - 签名有效性检查（零方差维度比例、描述子塌缩率）。

## 4. 资源与算力

- 论文未明确说明使用的 GPU 型号、数量或训练/运行时长。仅提及实验在四个预训练模型上完成，这些模型本身是公开的冻结检查点，无需训练。由于是嵌入层响应计算（无需前向传播或反向传播），计算负载相对轻量，但论文未提供具体硬件细节。

## 5. 实验数量与充分性

- **实验数量**：
  - 主要比较实验：4 种相似性度量 × 4 模型，得到最近邻分配；
  - Bootstrap 稳定性：200 次重采样 × 2 种相关性度量；
  - 有效性检查：800 维签名零方差比例，以及每个模型的描述子塌缩率。
- **充分性**：
  - **优点**：实验设计包含稳定性检验和退化诊断，提升了结果可信度。
  - **不足**：模型数量极少（4个），家族结构简单（2×2），无消融研究（未单独验证每个通道/度量/参数网格的贡献），未与其他表示比较方法（如CKA、直接几何指标）对照，未评估提示分布变化的影响。因此实验结果仅是概念验证，不足以支撑泛化性结论。

## 6. 论文的主要结论与发现

- 皮尔逊、斯皮尔曼和余弦相似度均完全恢复四个模型的同族最近邻分配（准确率 1.00），且两个预期互配对被同时恢复（2/2）。
- 欧氏距离恢复 3/4 的同族分配和 1/2 的互配对。
- 在 200 次配对 bootstrap 中，皮尔逊和斯皮尔曼的组内相似度均大于组间相似度，且两个互配对被全部恢复（频率 1.00），说明结果对提示重采样稳定。
- 签名有效性：800 维中仅 60 维零方差（0.075），描述子塌缩率在 5.0%–5.4% 之间，低于预设阈值，说明响应未退化。
- 结论：确定性神经混沌响应表面足以在有限模型群中恢复预期的模型家族关系，但不可外推至通用家族识别或混沌方法的必要性。

## 7. 优点

- **方法创新性**：将神经混沌描述子从“分类特征提取器”拓展为“表示空间探针”，提出响应式指纹概念，为嵌入分析提供了新颖视角。
- **任务无关性**：不训练辅助分类器，不依赖下游任务，直接刻画嵌入空间本身。
- **跨模型可比性**：通过固定参数网格和统一签名维度，解决了不同词汇表/嵌入维度/分词器导致的不可直接比较问题。
- **稳定性验证**：采用配对 bootstrap 检验提示级稳定性，并设计零方差/塌缩率作为退化诊断，方法严谨。
- **开源代码**：提供了完整实现和实验配置，可复现。

## 8. 不足与局限

- **模型群过小**：仅 4 个检查点、2 个家族，结果偶然性风险高。
- **无法归因机制**：未隔离嵌入几何、分词器、训练目标、师生关系等因素对家族恢复的贡献。
- **无消融实验**：未评估单通道（FR/ENT）、单度量、参数网格尺寸或混沌映射替代方案的影响，无法证明混沌成分的必要性或优越性。
- **泛化性未验证**：未测试其他提示集、更长上下文、多语言、领域文本或对抗样本；bootstrap 仅反映当前提示集内的稳定性。
- **比较方法单一**：未与现有表示相似度方法（如标准CKA、SVCCA）或直接几何指标进行对比。
- **解释阈值武断**：表4中的阈值是描述性指南，缺乏统计校准。
- **欧氏距离表现弱**：表明签名距离度量选择敏感，但未深入分析原因。
- **无下游验证**：未评估该方法对模型选择、调试或安全性应用的实际价值。

（完）
