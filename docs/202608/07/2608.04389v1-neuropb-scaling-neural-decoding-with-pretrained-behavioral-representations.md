---
title: "NeuroPB: Scaling Neural Decoding with Pretrained Behavioral Representations"
title_zh: NeuroPB：利用预训练行为表示扩展神经解码
authors: "Luyao Jin, Yonghao Song, Huan Zhao, Vincent C. K. Cheung, Wei-Hsin Liao"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04389v1"
tags: ["query:neuro-mental"]
score: 9.0
evidence: 面向脑机接口的神经解码
tldr: "神经解码受限于神经数据规模小且异质性高，而行为数据可大规模获取。NeuroPB框架先在大规模运动行为数据上预训练运动编码器，再将神经活动对齐到该行为表示空间，用少量配对数据优化神经编码器和轻量解码器。在多个猕猴数据集上，行为预训练使轨迹解码R²提升11%（中心出）和8%（随机目标），且机器人轨迹预训练效果媲美猕猴数据。该工作证明行为预训练是可扩展的神经解码来源，为有限神经数据下高性能、低校准BCI提供新路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.04389v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 799, \"height\": 294, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04389v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1451, \"height\": 712, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04389v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 857, \"height\": 398, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04389v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 857, \"height\": 401, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04389v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 831, \"height\": 997, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04389v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 848, \"height\": 295, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04389v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 881, \"height\": 619, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04389v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 851, \"height\": 799, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.04389v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 794, \"height\": 460, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04389v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 610, \"height\": 342, \"label\": \"Table\"}]"
motivation: 当前神经解码受限于神经数据规模和异质性，而行为数据易大规模获取，可迁移到神经解码。
method: 在大规模行为数据上预训练运动编码器，用少量配对神经-行为数据对齐表示空间，再联合优化神经编码器与轻量解码器重建运动。
result: "跨多个猕猴数据集，行为预训练使中心出任务R²提升11%、随机目标提升8%；机器人预训练媲美猕猴，且数据量越大增益越明显。"
conclusion: "行为预训练为神经解码提供可扩展知识来源，显著提升跨会话、跨任务泛化，实现仅需10%校准即可匹配从头训练。"
---

## 摘要
从神经活动解码连续运动轨迹对于开发实用的脑机接口（BCI）至关重要。然而，当前的神经解码器受到神经记录数据规模有限和异质性的制约。相比之下，行为数据可以从人类、动物、仿真和机器人系统中以更大规模更便捷地收集。在此，我们引入NeuroPB，一个通过迁移预训练行为表示的知识来扩展神经解码的框架。NeuroPB首先在大规模运动行为数据上预训练一个运动编码器，然后利用有限的神经-行为配对记录，将神经活动与所得行为表示空间对齐。随后，优化一个神经编码器和一个轻量级运动解码器，以从对齐的神经表示中重建连续运动。在多个猕猴运动数据集上，行为预训练提高了轨迹解码性能，其中在中心外出任务中R²提高了11%，在随机目标任务中提高了8%，相比从头训练运动编码器。值得注意的是，在机器人轨迹上预训练所达到的性能与在猕猴轨迹上预训练相当，表明可迁移的运动学结构在生物和人工模型之间共享。此外，当神经数据量固定时，解码性能随着机器人预训练数据的规模和多样性增加而提高。预训练还增强了对记录会话、受试者和运动任务的泛化能力，仅需10%的校准即可匹配从头训练。总体而言，这些结果确立了行为预训练作为神经解码可扩展来源的地位，并为在有限神经数据下实现高性能和高校准效率的脑机接口提供了一条有前景的途径。

## Abstract
Decoding continuous motor trajectories from neural activity is essential for developing practical brain-computer interfaces (BCIs). However, current neural decoders are constrained by the limited scale and heterogeneity of neural recordings. In contrast, behavioral data can be collected more readily and at substantially larger scale from humans, animals, simulations, and robotic systems. Here, we introduce NeuroPB, a framework that scales neural decoding by transferring knowledge from pretrained behavioral representations. NeuroPB first pretrains a motor encoder on large-scale motor behavior data and then aligns neural activity with the resulting behavioral representation space using a limited set of paired neural-behavioral recordings. A neural encoder and lightweight motor decoder are subsequently optimized to reconstruct continuous movement from the aligned neural representations. Across multiple macaque motor datasets, behavioral pretraining improves trajectory decoding, including an 11% $R^2$ increase on center-out and 8% on random-target compared with training the motor encoder from scratch. Notably, pretraining on robotic trajectories achieves performance comparable to pretraining on macaque trajectories, demonstrating that transferable kinematic structure is shared across biological and artificial models. Moreover, decoding performance improves as the scale and diversity of robotic pretraining data increase, when the amount of neural data is fixed. Pretraining also enhances generalization across recording sessions, subjects, and motor tasks, with only 10% calibration needed to match training from scratch. Overall, these results establish behavioral pretraining as a scalable source for neural decoding and provide a promising route toward high-performance and calibration-efficient BCIs under limited neural data.

---

## 论文详细总结（自动生成）

# NeuroPB：利用预训练行为表示扩展神经解码 —— 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- 连续运动轨迹解码是实用脑机接口（BCI）的关键能力，但当前神经解码器受限于神经数据的**规模小**和**异质性高**。
- 神经数据采集成本高、技术难度大，且不同记录会话、个体、脑区、设备之间差异显著，导致模型泛化能力差。
- 相比之下，**行为数据**可以从人类、动物、仿真和机器人系统中大规模、低成本地获取。
- 作者提出假设：行为本身包含可迁移的运动先验，利用大规模行为轨迹预训练出的表示空间，可以引导神经编码器学习更具迁移性的表征，从而在有限神经数据下提升解码性能。
- 该工作的核心意义在于：**将行为预训练作为神经解码的可扩展知识来源**，为高性能、校准高效的BCI提供了一条互补于传统神经预训练的新路径。

## 2. 方法论

### 核心思想
- 通过一个**预训练行为表示空间**作为“锚点”，将神经活动对齐到该空间中，使神经编码器提取的行为相关信息更丰富，并抑制记录特异性变异。

### 三阶段流程
1. **运动编码器预训练（Motor Encoder Pretraining）**
   - 基于 Transformer 结构，通过**掩码轨迹重建**学习运动结构。
   - 输入轨迹段 \( P=[p_1,\dots,p_T] \)，随机掩码部分时间步，用可学习掩码嵌入和位置编码构造输入序列，经编码器得到上下文化 token，再用轻量预测头重建被掩码的位置坐标。
   - 目标函数为 \( \mathcal{L}_{traj} = \frac{1}{|\mathcal{M}|}\sum_{t\in\mathcal{M}}\|\hat{p}_t - p_t\|^2_2 \)。
   - 预训练后冻结编码器，输出聚合后的归一化行为表示 \( z_m \)。
2. **神经-行为模态对齐（Modality Alignment）**
   - 神经编码器基于 Perceiver 架构（类似 POYO），将尖峰事件序列处理为固定数量的潜在 token。
   - 使用旋转位置嵌入（RoPE）编码时间信息，通过交叉注意力聚合尖峰信息，并经多层自注意力得到神经表示 \( Z_L \)。
   - 神经表示经投影头降维并 ℓ2 归一化得到 \( z_n \)。
   - 使用 **CLIP 风格对称对比损失**对齐配对样本：\( \mathcal{L}_{align} = \frac{1}{2}(\mathcal{L}_{n\to m} + \mathcal{L}_{m\to n}) \)，拉近配对神经-行为表示、推开非配对样本。
3. **运动解码器训练（Motor Decoding）**
   - 使用轻量 MLP 解码器从对齐后的神经表示 \( z_n \) 重建二维连续轨迹，损失为 MSE 位置误差。
   - 总损失为 \( \mathcal{L} = \mathcal{L}_{align} + \mathcal{L}_{motor} \)，联合优化神经编码器、投影头和解码器，冻结预训练运动编码器。
- 推理时仅需神经编码器和解码器，无需行为输入。

## 3. 实验设计

- **数据集**：Perich et al., 2018 的猕猴运动数据集，包含四只恒河猴，记录自 M1 和 PMd 脑区的多电极阵列；两个任务：
  - **Center-Out (CO)**：中心向外围8个目标移动。
  - **Random-Target (RT)**：随机目标连续追踪。
- **基准方法对比**：Wiener Filter、Smoothing、DenseNN、RNN、LFADS、NEDS、NDT2、POYO 等经典/近期模型，其中部分基线结果引用自其他论文。
- **预训练策略对比**：
  - Scratch（从头训练）
  - Macaque-PT（猕猴轨迹预训练）
  - Robot-Matched-PT（与猕猴轨迹规模匹配的 LIBERO-Spatial 机器人轨迹预训练）
  - Robot-Large-PT（大规模 LIBERO-100 机器人轨迹预训练）
- **主要实验场景**：
  - 单会话解码性能对比。
  - 行为预训练与从头训练的效果对比。
  - 预训练数据源（生物 vs 机器人）与数据规模的影响。
  - 校准效率验证（跨会话、跨主题、跨任务 CO-RT 和 RT-CO 四种迁移设置，在 1%–100% 目标数据下）。6
  - 可视化分析（表示相似性矩阵、PCA）。
  - 消融实验（去除对比学习、单元编码、RoPE、交叉注意力、自注意力）。

## 4. 资源与算力

- 文中明确提到实现基于 **PyTorch**，在 **单块 RTX 4090 GPU** 上完成。
- 使用了 SparseLamb 优化器，批大小 128，最大学习率 0.04，余弦退火调度。
- **但作者未明确说明训练总时长、GPU 数量、神经预训练成本或具体耗能**，因此算力需求无法从论文中完整量化。

## 5. 实验数量与充分性

- 实验数量较充足：
  - CO 和 RT 两个任务上的基准对比（8种基线）。
  - 三种预训练条件与 Scratch 的对比（含显著性检验）。
  - 数据源和数据规模对比（3种预训练策略，2个维度）。
  - 四种迁移设置的校准效率曲线（多组低数据比例实验）。
  - 两类可视化分析。
  - 5项消融实验（共6种配置）。
- 充分性评价：
  - **优点**：覆盖了行为预训练的核心假设（来源、规模、迁移、校准、机制），实验设计较为系统。
  - **局限**：
    - 部分基线结果（NEDS、NDT2、POYO）引用其他文献，可能存在数据集划分或预处理不一致的风险，基准对比的公平性需谨慎对待。
    - 视觉化分析仅基于6个会话的PCA，证据强度有限。
    - 消融实验仅在 CO 任务上进行，未覆盖 RT 任务。
    - 未报告多次随机种子的完整方差（仅部分指标给出标准差），统计可靠性有待加强。

## 6. 主要结论与发现

1. **行为预训练显著提升解码性能**：相比从头训练，Robot-Large-PT 在 CO 上 R² 提升约 10.6%（94.78% vs 84.19%），在 RT 上提升约 8.5%（84.75% vs 76.29%），且方差更小。
2. **机器人轨迹预训练可与生物轨迹媲美**：规模匹配时 Macaque-PT 与 Robot-Matched-PT 性能相当，表明运动结构具有跨实体可迁移性。
3. **扩大机器人预训练数据规模带来持续增益**：Robot-Large-PT 优于规模匹配条件，说明数据的规模和多样性是关键因素。
4. **预训练提升泛化与校准效率**：在跨会话、跨主题、跨任务设置下，预训练模型显著优于从头训练；仅需约 10% 目标校准数据即可达到从头训练100%数据的性能。
5. **对比学习对齐有效**：可视化显示对齐后神经表示按运动方向聚类、跨会话变异减少。

## 7. 优点

- **创新视角**：跳出神经预训练的资源瓶颈，利用大规模行为数据（尤其机器人数据）为神经解码提供可扩展先验，是明显的方法论创新。
- **简易而有效的框架**：三阶段流程清晰，预训练模型冻结后对齐，推理无需行为输入，工程上易于部署。
- **跨实体迁移验证扎实**：通过规模匹配控制，干净地分离了行为来源与数据规模的影响，论证逻辑严谨。
- **全面关注泛化问题**：不仅评测单会话性能，还系统验证了跨会话、跨主体、跨任务迁移，并揭示了校准效率优势，非常贴近实际BCI需求。
- **实现细节透明**：附录提供了网络架构、超参数、算法流程和校准细节，便于复现。

## 8. 不足与局限

- **实验范围有限**：仅使用猕猴侵入式神经记录，未在人类数据或非侵入性信号（EEG、fMRI等）上验证，结论的普适性存疑。
- **离线评估局限**：所有实验均为离线解码，未在实时闭环BCI条件下验证，忽略了神经活动非平稳性、用户反馈延迟等实际问题。
- **机器人数据转换可能引入噪声**：LIBERO轨迹经CoTracker处理获得点跟踪轨迹，可能过滤掉真实的运动学细节，与猕猴数据分布存在差异（但实验显示影响较小）。
- **基准公平性问题**：部分对比方法的结果来自其他论文，数据集划分和超参数可能不一致，对比客观性有一定风险。
- **未提供误差细粒度分析**：仅报告R²，未对轨迹误差（如速度、加速度、目标方向敏感性）进行深入分解。
- **算力消耗未报告**：无法评估大规模行为预训练的实际成本，可能影响方法在资源受限场景下的可行性。

（完）
